using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Class that marks Object that are placed inside the scene and can therefore be edited or deleted (by the Edit and elete Modifier Script)
/// </summary>
public class SceneAsset : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
